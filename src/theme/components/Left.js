import variable from './../variables/platform';

export default (_variables = variable) => {
	const leftTheme = {
		flex: 1,
		alignSelf: 'center',
		alignItems: 'flex-start',
	};

	return leftTheme;
};
